# 本地任務看板（GitHub Projects 未連線前的 fallback）
#
# 依 root README「錯誤處理與退化策略」：任務追蹤工具故障時記錄到本地 tasks.yaml。
# GitHub MCP Server（github）已寫進 .kiro/settings/mcp.json（原生 binary、免 Docker），
# 但在下載 github-mcp-server binary 並填入 PAT 實際連上前，先以此檔案作為預設任務追蹤方式；
# 連上 GitHub Projects 後由 producer 協助遷移。
#
# 格式沿用 .kiro/steering/global/contracts.md 的 Task Contract。

tasks: []
